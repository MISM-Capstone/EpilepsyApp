import { StyleSheet } from "react-native";

const PickerStyles = StyleSheet.create({
    inputIOS: {
        fontSize: 18, 
        borderWidth: 1,
        borderColor: '#888',
        borderRadius: 4,
        color: 'black',
        paddingRight: 30,
        padding: 8
    }
})

export default PickerStyles;