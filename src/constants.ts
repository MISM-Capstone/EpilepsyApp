export const STORAGE_KEYS = {
    survey: "@survey_resp",
    userToken: "@user_token"
}

export const CONTEXT_OPTIONS = {
    login: "login",
    logout: "logout",
    restoreToke: "restoreToken"
}

export const COLORS = {
    darkBlue: "#2E4D9E",
}